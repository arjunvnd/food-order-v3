enum Role {
    SUPER_ADMIN
    ADMIN
    VENDOR
}

enum OrderStatus {
    PENDING
    ACCEPTED
    REJECTED
    COMPLETED
}

enum PaymentStatus {
    UNPAID
    PAID
}

enum VendorType {
    MALL_VENDOR
    STANDALONE
    TAKEAWAY
}

enum OrderType {
    DINE_IN
    TAKEAWAY
}
